#pragma once

#include <Arduino.h>

constexpr uint8_t SENSOR_ID_LEN = 17;       // 16 chars + null terminator
constexpr uint8_t SENSOR_NAME_LEN = 33;     // 32 chars + null terminator
constexpr uint8_t MAX_SENSORS = 16;
